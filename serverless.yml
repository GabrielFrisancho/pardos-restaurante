org: gabrielfrisancho
service: pardos-restaurante

custom:
  pythonRequirements:
    pythonBin: python3
    dockerizePip: false
    useDownloadCache: false
    useStaticCache: false
  ordersTable: pardos-restaurante-${sls:stage}-orders
  stepsTable: pardos-restaurante-${sls:stage}-steps

provider:
  name: aws
  runtime: python3.13
  region: us-east-1
  timeout: 29
  iam:
    role: arn:aws:iam::213965374161:role/LabRole
  environment:
    ORDERS_TABLE: ${self:custom.ordersTable}
    STEPS_TABLE: ${self:custom.stepsTable}
    EVENT_BUS_NAME: pardos-restaurante-${sls:stage}-event-bus

functions:
  # ORQUESTADOR MS - Escucha eventos de api-clientes
  iniciarOrquestacion:
    handler: orquestador/handler.iniciar_orquestacion
    events:
      - eventBridge:
          eventBus: ${self:provider.environment.EVENT_BUS_NAME}
          pattern:
            source:
              - cliente-ms
            detail-type:
              - OrderCreated

  # ETAPAS MS - Endpoints manuales
  iniciarEtapa:
    handler: etapas/handler.iniciar_etapa
    events:
      - http:
          path: /etapas/iniciar
          method: post
          cors: true
          integration: lambda # Como en tu ejemplo

  completarEtapa:
    handler: etapas/handler.completar_etapa
    events:
      - http:
          path: /etapas/completar
          method: post
          cors: true
          integration: lambda # Como en tu ejemplo

  # DASHBOARD MS
  obtenerResumen:
    handler: dashboard/handler.obtener_resumen
    events:
      - http:
          path: /dashboard/resumen
          method: get
          cors: true
          integration: lambda

  obtenerMetricas:
    handler: dashboard/handler.obtener_metricas
    events:
      - http:
          path: /dashboard/metricas
          method: get
          cors: true
          integration: lambda

  obtenerPedidos:
    handler: dashboard/handler.obtener_pedidos
    events:
      - http:
          path: /dashboard/pedidos
          method: get
          cors: true
          integration: lambda

resources:
  Resources:
    OrdersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.ordersTable}
        AttributeDefinitions:
          - AttributeName: PK
            AttributeType: S
          - AttributeName: SK
            AttributeType: S
        KeySchema:
          - AttributeName: PK
            KeyType: HASH
          - AttributeName: SK
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST

    StepsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.stepsTable}
        AttributeDefinitions:
          - AttributeName: PK
            AttributeType: S
          - AttributeName: SK
            AttributeType: S
        KeySchema:
          - AttributeName: PK
            KeyType: HASH
          - AttributeName: SK
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST

    EventBus:
      Type: AWS::Events::EventBus
      Properties:
        Name: ${self:provider.environment.EVENT_BUS_NAME}
